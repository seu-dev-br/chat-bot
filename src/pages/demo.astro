---
// src/pages/demo.astro
---
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Demonstração - Chatbot Virtual</title>
    <style>
      body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .demo-container {
        text-align: center;
        max-width: 800px;
        padding: 40px;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      }

      .demo-container h1 {
        font-size: 3rem;
        margin-bottom: 20px;
        background: linear-gradient(45deg, #fff, #f0f0f0);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .demo-container p {
        font-size: 1.2rem;
        margin-bottom: 30px;
        opacity: 0.9;
        line-height: 1.6;
      }

      .demo-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        flex-wrap: wrap;
      }

      .demo-btn {
        background: linear-gradient(45deg, #ff6b6b, #ee5a24);
        color: white;
        border: none;
        padding: 15px 30px;
        font-size: 1.1rem;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s;
        font-weight: 600;
        text-decoration: none;
        display: inline-block;
      }

      .demo-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 30px rgba(255, 107, 107, 0.4);
      }

      .demo-btn.secondary {
        background: linear-gradient(45deg, #667eea, #764ba2);
      }

      .demo-btn.secondary:hover {
        box-shadow: 0 15px 30px rgba(102, 126, 234, 0.4);
      }

      .status-indicator {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        background: rgba(255, 255, 255, 0.2);
        padding: 10px 20px;
        border-radius: 25px;
        margin-bottom: 30px;
        backdrop-filter: blur(10px);
      }

      .status-dot {
        width: 12px;
        height: 12px;
        background: #4CAF50;
        border-radius: 50%;
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% { opacity: 1; }
        50% { opacity: 0.5; }
        100% { opacity: 1; }
      }

      .features-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin: 40px 0;
        text-align: left;
      }

      .feature-item {
        background: rgba(255, 255, 255, 0.1);
        padding: 20px;
        border-radius: 10px;
        border: 1px solid rgba(255, 255, 255, 0.2);
      }

      .feature-item h3 {
        margin-top: 0;
        color: #fff;
        font-size: 1.1rem;
      }

      .feature-item p {
        margin-bottom: 0;
        opacity: 0.8;
        font-size: 0.9rem;
      }

      @media (max-width: 768px) {
        .demo-container {
          margin: 20px;
          padding: 30px 20px;
        }

        .demo-container h1 {
          font-size: 2rem;
        }

        .demo-buttons {
          flex-direction: column;
          align-items: center;
        }

        .features-list {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <div class="demo-container">
      <div class="status-indicator">
        <div class="status-dot"></div>
        <span>Sistema Online e Funcionando</span>
      </div>

      <h1>🚀 Plataforma Chatbot Virtual</h1>
      <p>
        Demonstração completa do sistema de atendimento inteligente.
        Interface moderna, responsiva e totalmente funcional.
      </p>

      <div class="features-list">
        <div class="feature-item">
          <h3>💬 Chat Inteligente</h3>
          <p>Widget de chat com IA integrada e respostas contextuais</p>
        </div>
        <div class="feature-item">
          <h3>📱 Multi-canal</h3>
          <p>Integração com WhatsApp e interface web unificada</p>
        </div>
        <div class="feature-item">
          <h3>🛠️ Painel Admin</h3>
          <p>Dashboard completo para gestão e configurações</p>
        </div>
        <div class="feature-item">
          <h3>📚 Base de Dados</h3>
          <p>Sistema de conhecimento organizado e pesquisável</p>
        </div>
      </div>

      <div class="demo-buttons">
        <a href="/" class="demo-btn">🏠 Página Principal</a>
        <a href="/admin" class="demo-btn secondary">🛠️ Painel Admin</a>
        <a href="/knowledge" class="demo-btn">📚 Base de Conhecimento</a>
      </div>

      <div style="margin-top: 40px; opacity: 0.7; font-size: 0.9rem;">
        <p>💡 <strong>Dica:</strong> Clique no botão de chat (💬) no canto inferior direito para testar o atendimento virtual</p>
      </div>
    </div>
  </body>
</html>
